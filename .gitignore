# macOS system files
.DS_Store

# Python bytecode files
__pycache__/
*.pyc

# postgres-connect local configs (do not commit secrets)
skills/.curated/postgres-connect/env.yaml
skills/.curated/postgres-connect/config.yaml
skills/.curated/postgres-connect/*.local.yaml

# local env config (project root)
env.yaml
