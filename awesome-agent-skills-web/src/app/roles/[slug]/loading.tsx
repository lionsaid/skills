import { PageLoading } from "@/components/page-loading";

export default function Loading() {
  return (
    <PageLoading
      title="Loading role"
      description="Preparing the recommended skills for this role."
    />
  );
}
