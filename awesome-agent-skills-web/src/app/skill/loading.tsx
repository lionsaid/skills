import { PageLoading } from "@/components/page-loading";

export default function Loading() {
  return (
    <PageLoading
      title="Loading skill"
      description="Fetching the skill details and install instructions."
    />
  );
}
